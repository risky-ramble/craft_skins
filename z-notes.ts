/*
    UNIT TESTS
      initialize()

      create_recipe()
        invalid Pubkey to ingredient_mints
        invalid ingredient_amounts (<0 || >2^64)
        invalid recipe account (can't store mints, amounts)
        invalid collection NFT
        
      add_skin()

      craft_skin()
*/